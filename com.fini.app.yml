id: com.fini.app
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: fini-app

finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --filesystem=home
  - --talk-name=org.freedesktop.Flatpak
  - --talk-name=org.freedesktop.Notifications

modules:
  - name: fini
    buildsystem: simple
    build-commands:
      - install -Dm755 fini-app /app/bin/fini-app
      - install -Dm644 com.fini.app.desktop /app/share/applications/com.fini.app.desktop
      - install -Dm644 icon.png /app/share/icons/hicolor/128x128/apps/com.fini.app.png
    sources:
      - type: file
        path: src-tauri/target/release/fini-app
        dest-filename: fini-app
      - type: file
        path: src-tauri/icons/128x128.png
        dest-filename: icon.png
      - type: file
        path: com.fini.app.desktop
