{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window during e2e tests",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "dialog:default",
    "opener:default",
    "notification:default",
    "playwright:default"
  ]
}
